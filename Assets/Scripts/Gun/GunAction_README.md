# GunAction.cs

키 입력으로 총 애니메이션을 제어하는 스크립트

## 📋 개요

키보드 입력을 받아 Animator의 파라미터를 설정하여 총 관련 애니메이션을 재생합니다. 발사, 재장전, 은폐, 무기 꺼내기 등의 동작을 제어합니다.

## 🎯 주요 기능

- **발사 애니메이션**: A 키로 발사 애니메이션 제어
- **재장전 애니메이션**: B 키로 재장전 트리거
- **은폐 애니메이션**: C 키로 은폐 모션 제어
- **무기 꺼내기**: D 키로 무기 꺼내기 애니메이션
- **발사 트리거**: Q 키로 발사 트리거

## 🔧 적용 방법

### 1단계: 컴포넌트 추가
1. 총 오브젝트 (또는 Gun_Kriss prefab) 선택
2. Inspector에서 `Add Component` → `Gun Action` 추가

### 2단계: 필수 설정
- **Controller Gun Kriss**: Animator 컴포넌트 할당
  - 총 오브젝트에 Animator 컴포넌트가 있어야 함
  - Animator Controller가 할당되어 있어야 함

## ⚙️ Inspector 파라미터

| 파라미터 | 타입 | 설명 |
|---------|------|------|
| Controller Gun Kriss | Animator | 총 애니메이션을 제어할 Animator 컴포넌트 |

## 🎮 사용 키

| 키 | 기능 | 타입 | 설명 |
|---|---|---|---|
| **A** | 발사 | Bool (누르고 있음) | 누르고 있으면 계속 발사 (IsShooting = true) |
| **B** | 재장전 | Trigger | 한 번 누르면 재장전 애니메이션 실행 |
| **C** | 은폐 | Bool (누르고 있음) | 누르고 있으면 은폐 모션 (IsHiding = true) |
| **D** | 무기 꺼내기 | Trigger | 한 번 누르면 무기 꺼내기 애니메이션 실행 |
| **Q** | 발사 트리거 | Trigger | 한 번 누르면 발사 트리거 실행 |

## 🎬 Animator Controller 설정

다음 파라미터들을 Animator Controller에 추가해야 합니다:

### Bool 파라미터
- **IsShooting**: 발사 상태 (A 키)
- **IsHiding**: 은폐 상태 (C 키)

### Trigger 파라미터
- **OnReload**: 재장전 트리거 (B 키)
- **OnDraw**: 무기 꺼내기 트리거 (D 키)
- **OnFire**: 발사 트리거 (Q 키)

### 상태 전환 예시
```
Idle → Shooting (IsShooting = true)
Idle → Hiding (IsHiding = true)
Any State → Reload (OnReload trigger)
Any State → Draw (OnDraw trigger)
Any State → Fire (OnFire trigger)
```

## 🔄 동작 원리

### Bool 파라미터 (A, C 키)
- **KeyDown**: 파라미터를 `true`로 설정
- **KeyUp**: 파라미터를 `false`로 설정
- 누르고 있는 동안 계속 `true` 유지

### Trigger 파라미터 (B, D, Q 키)
- **KeyDown**: 트리거 한 번 실행
- 한 번만 실행되므로 연속 입력 시에도 한 번만 작동

## 🐛 문제 해결

### 애니메이션이 재생되지 않을 때
1. **Animator 확인**
   - Inspector에서 `Controller Gun Kriss` 필드에 Animator 할당
   - Animator Controller가 할당되어 있는지 확인

2. **파라미터 확인**
   - Animator Controller에 필요한 파라미터가 있는지 확인
   - 파라미터 이름이 정확한지 확인:
     - IsShooting (Bool)
     - IsHiding (Bool)
     - OnReload (Trigger)
     - OnDraw (Trigger)
     - OnFire (Trigger)

3. **상태 전환 확인**
   - Animator Controller에서 상태 전환 조건 확인
   - 파라미터가 올바르게 연결되었는지 확인

### 키 입력이 작동하지 않을 때
1. **키 확인**
   - Unity Input Manager에서 키 설정 확인
   - 다른 스크립트와 키 충돌 확인

2. **스크립트 활성화 확인**
   - Inspector에서 스크립트가 활성화되어 있는지 확인
   - 오브젝트가 활성화되어 있는지 확인

## 📝 코드 예시

### Animator 직접 제어
```csharp
// 발사 시작
animator.SetBool("IsShooting", true);

// 발사 종료
animator.SetBool("IsShooting", false);

// 재장전
animator.SetTrigger("OnReload");
```

### 키 변경
```csharp
// A 키 대신 다른 키 사용
if (Input.GetKeyDown(KeyCode.Mouse0)) // 마우스 왼쪽 버튼
{
    controller_gun_kriss.SetBool("IsShooting", true);
}
```

## 🔄 확장 가능성

### 추가 기능
- **연사 모드**: 단발/연사 모드 전환
- **조준**: 우클릭으로 조준 모드
- **장전 수 표시**: 남은 탄약 수 표시
- **무기 교체**: 여러 무기 간 전환

### 추가 키
- **R**: 재장전 (대체)
- **마우스 왼쪽**: 발사
- **마우스 오른쪽**: 조준

## 📚 관련 파일
- Animator Controller: 총 애니메이션 제어
- Gun_Kriss.prefab: 총 prefab

